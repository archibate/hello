name: File updater
on:
  push:
    branches: master

jobs:
  update_commit_id:
    name: Update my file
    runs-on: ubuntu-latest
    steps:
      uses: actions/checkout@v2
      run: |
        git status
        echo hello > hello.txt
        git add hello.txt
        git commit -m "Update my file"
        git push
